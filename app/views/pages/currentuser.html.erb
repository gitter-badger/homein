<% content_for(:body_attributes) do %>
  data-no-turbolink="true"
<% end %>

<h1><%= current_user.email %></h1>

<% current_user.places.each do |place| %>
	<li>
		<%= link_to place.address, place_path(place) %> | <%= place.description %>
	</li>
<% end %>