<%= javascript_tag do %>
    window.image_urls = []
    
    window.image_thumb_urls = []
    
    <% @place.pictures.each do |picture| %>
        window.image_urls.push("<%= picture.image.url %>")
        
        window.image_thumb_urls.push("<%= picture.image.url(:thumb) %>")
    <% end %>
<% end %>